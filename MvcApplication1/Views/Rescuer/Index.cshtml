@model IEnumerable<MvcApplication1.Models.Rescuer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.WorkPhone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmergencyTeam.EmergencyTeamId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HeadOfGroup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MedicalBoardPassageDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.WorkPhone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmergencyTeam.EmergencyTeamId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HeadOfGroup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MedicalBoardPassageDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.UserId}) |
            @Html.ActionLink("Details", "Details", new { id = item.UserId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.UserId })
        </td>
    </tr>
}

</table>
