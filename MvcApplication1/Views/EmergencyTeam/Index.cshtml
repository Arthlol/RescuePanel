@model IEnumerable<MvcApplication1.Models.EmergencyTeam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CreateDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmergencyTeamName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmergencyTeamName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmergencyTeamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmergencyTeamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmergencyTeamId })
        </td>
    </tr>
}

</table>
